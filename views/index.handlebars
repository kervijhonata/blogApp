<div class="card bg-primary text-light" id="heading-jumbotron">
    <div class="card-body">
        <h1 class="display-4">Bem vindo ao Blog!</h1>
        <p class="lead">Este é um blog simples, desenvolvido com Node.js + Express.js + MongoDB.</p>
        <hr>
        <p class="my-4">Estamos sempre atentos às novidades do mercado de tecnologia e do mundo da programação.</p>
        <p class="lead">
            <a href="#" class="btn btn-light btn-lg" role="button">Crie sua conta já!</a>
        </p>
    </div>
</div>
<h2 class="mt-4 mb-4">Postagens recentes:</h2>
{{#each postagens}}
    <div class="card mt-4 mb-4">
        <div class="card-body">
            <h3>{{titulo}}</h3>
            <p>{{descricao}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-primary">Leia mais</button></a>
            <hr>
            {{#if categoria.nome}}
                <small>{{categoria.nome}}</small>
            {{else}}
                <small>Categoria Inexistente</small>
            {{/if}}
            <small>{{data}}</small>
        </div>
    </div>
{{else}}
    <p>Nenhuma postagem registrada</p>
{{/each}}